/**
 * Domain objects
 *
 * Pure domain models for the application layer.
 * These are independent of infrastructure and orchestration logic.
 */

export * from './types';
export { ApplicationParticipant } from './Participant';
